<a href="/user/index">Back to User Index</a>
<a class="edit" href="/user/{{user._id}}/edit">Edit Profile</a>
<p>Name: {{user.name}}</p>
<p>Email: {{user.local.email}}</p>
<p>Location: {{user.location}}</p>
<img class="profile-img" src="{{user.photo}}" alt="Photo of {{user.name}}">
<p>Interests:</p>
<div class="btn-group">
{{#each user.interests}}
  <div class="btn btn-primary">{{this}}</div>
{{/each}}
</div>

<p>Bio: {{user.bio}}</p>


<script>
  $(document).ready(function() {
    var currentUserId = "{{currentUser._id}}";
    var userPageId = "{{user._id}}"
    if (currentUserId != userPageId) {
      $(".edit").hide();
    }
  });
</script>
